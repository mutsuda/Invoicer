<%= simple_form_for(@job) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :price %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
